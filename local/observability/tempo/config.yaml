server:
  http_listen_port: 3200

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: 0.0.0.0:4317

storage:
  trace:
    backend: s3
    s3:
      endpoint: d3.minio.local:9000
      bucket: tempo
      access_key: d3fix4m
      secret_key: d3d3d3d3
      insecure: true