kind: Service
apiVersion: v1
metadata:
  name: prometheus
  namespace: monitoring
spec:
  selector:
    app: prometheus
    statefulset.kubernetes.io/pod-name: prometheus-0
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
  type: ClusterIP