apiVersion: v1
kind: Service
metadata:
  name: tempo
  namespace: monitoring
spec:
  type: ClusterIP
  ports:
    - port: 3200
      name: http
      targetPort: 3200
    - port: 4318
      name: otlp-http
      targetPort: 4318
  selector:
    app: tempo